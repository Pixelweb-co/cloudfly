version: "3.8"
services:
  cloudfly-tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cloudfly-tests
    command: ["python", "-m", "pytest", "-q"]
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # expose any needed ports (none for tests)
